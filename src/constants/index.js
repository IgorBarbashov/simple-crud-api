const { ENV_VARIABLES } = require('./envVariables');
const { HTTP_METHODS } = require('./httpMethods');
const { HTTP_HEADERS } = require('./httpHeaders');
const { ROUTES } = require('./routes');
const { MODEL_TYPES } = require('./modelTypes');
const { ENTITIES } = require('./entities');
const { MESSAGES } = require('./messages');

module.exports = {
    ENV_VARIABLES,
    HTTP_METHODS,
    HTTP_HEADERS,
    ROUTES,
    MODEL_TYPES,
    ENTITIES,
    MESSAGES
};
